# KSD_AI — TODO

## Качество перевода (приоритет)

Текущая проблема: ChatGPT даёт слабый перевод в KSD-рамке.
Варианты решения (от лучшего к сложному):

### Вариант 1: Anthropic API (Claude) — РЕКОМЕНДУЕТСЯ
- Получить API-ключ на console.anthropic.com (pay-per-use)
- Переписать ksd_ai_translator.py под anthropic SDK (убрать Playwright полностью)
- Claude уже понимает KSD-методологию глубже ChatGPT
- ~$0.003 за шабд на claude-sonnet-4-6

### Вариант 2: Улучшение промпта для ChatGPT
- Переключиться на GPT-4o (более умная модель)
- Добавить 3-5 few-shot примеров с полным разбором прямо в промпт
- Проверить: может быть ChatGPT не видит весь system-блок (лимит контекста)

### Вариант 3: Ollama + готовая модель (без дообучения)
- Установить Ollama, скачать llama3.1:8b или mistral
- Адаптировать транслятор под Ollama API (http://localhost:11434)
- Плюсы: бесплатно, офлайн
- Минусы: качество ниже Claude/GPT-4

### Вариант 4: Ollama + fine-tune на KSD-корпусе
- Набрать датасет: пары (gurmukhi + sahib_singh_pa) → ksd_translation
- Использовать уже переведённые шабды (ksd_ang_json/) как training data
- Дообучить llama3 / mistral через Ollama или unsloth
- Требует GPU и ~50-100 переведённых шабдов для первого результата

### Вариант 5: Двухпроходный подход
- Проход 1: любой LLM генерирует черновик
- Проход 2: Claude/GPT-4 верифицирует по KSD-принципам и исправляет
- Дороже, но надёжнее

---

## Технические фиксы (выполнено)
- [x] NameError: rahao_vid → rahao_vids
- [x] TypeError: add_translation_with_blue() italic kwarg
- [x] Рахао-куплет: детекция всего куплета (не только последней строки)
- [x] Rebuild docx: re-apply find_rahao_vids при пересборке
- [x] Жирный шрифт для всего куплета Рахао

## Ананд Бани — незавершённое

- [ ] **66 строк без book-перевода** — список в `ANAND_KSD_BOOK/pending_verses.md`. Причина: либо страница пропущена, либо ChatGPT поставил перевод внутрь `word_meanings`-блока вместо `translation`-блока (паттерн: "Значение: текст"). Нужно добавить в `anand_build_ang_patches.py` fallback: если у gurbani-группы нет `translation`-блоков — поискать "Значение:" внутри `word_meanings`/`note`-блоков и взять текст после двоеточия. После — перепрогнать и задеплоить.
- [ ] **Пересъёмка и перепарсинг пропущенных страниц**: p011 p013 p014 p027 p036 p073 p079 — JSON повреждён (кривые кавычки / обрывы). Пересъёмка или ручная правка JSON, затем повторный прогон скрипта.
- [ ] **[[...]] в main для Ананда** — сейчас контекстные вставки KSD в Ананде есть только в `ksd_ai_gurbani_framework.context_note`. Если сайт умеет показывать `[[...]]` синим инлайн, подумать: стоит ли добавлять их в `ksd_ru.main` для новых book-переводов, или это ручная редакторская работа.

## Nitnem Mobile — фиксы

- [ ] **HTML в поле `comment` (confidence_reason)**: сейчас `Text()` рендерит комментарий как plain text — HTML-теги видны как символы. Нужно заменить `Text(text = line.comment, ...)` на `AndroidView { TextView }` с `Html.fromHtml(line.comment, Html.FROM_HTML_MODE_LEGACY)` для поля `comment`. Поле `context` (context_note) пока без изменений — там чистый текст.

- [ ] **Референс-бани (цитаты из СГГС в переводе)**: сейчас `SggsReferenceCard` кликабелен только если строка есть локально в данных. Если строка не найдена — нет ни выделения, ни клика на сайт. Нужно:
  - При `targetLine == null` рендерить карточку в отдельном стиле (например, зелёная рамка с иконкой ссылки) и по клику открывать `https://sikhizm.ru/sggs/?ang=N&line=N` через `Intent(Intent.ACTION_VIEW, Uri.parse(...))`.
  - Добавить явный `Text("Открыть на сайте →")` внутри карточки, чтобы пользователь видел, что это ссылка.
  - Проверить, что `WorkReferenceCard` тоже имеет визуальное выделение (рамка, цвет) достаточное чтобы отличаться от обычного текста.

- [ ] **Копирование строки**: длинный tap на `LineCard` (или кнопка-иконка в шапке карточки) копирует текст строки в буфер обмена в форматированном виде — гурмукхи, транслитерация, перевод — каждый на отдельной строке. Использовать `ClipboardManager`. Показать `Toast` "Скопировано".

- [ ] **Шэринг строки**: кнопка "Поделиться" в `LineCard` открывает стандартный Android share sheet. Текст для шэринга: гурмукхи + перевод + ссылка на сайт (`sikhizm.ru/sggs/?ang=N&line=N`). Использовать `Intent(Intent.ACTION_SEND)` с `type = "text/plain"`.

## Будущие фичи
- [ ] Подключить публикацию SGGS JSON на сайт через новый WordPress Import API без `git pull` для контента:
  - Endpoint: `POST https://sikhizm.ru/wp-json/sggs/v1/import`
  - Auth: `Authorization: Bearer <secret-token>`
  - Локальный token хранится в ignored файле `/home/royal/Work/Spiritual/sikhizm-ru/.env.local` как `SGGS_READER_IMPORT_TOKEN`; не печатать в чат и не коммитить.
  - На сервере хранится только SHA-256 hash token (`SGGS_READER_IMPORT_TOKEN_HASH` в `wp-config.php` или option `sggs_reader_import_token_hash`).
  - Payload schema: `schema_version: "1.0"`, `corpus_version`, optional `updates`, `files[]`.
  - Разрешенные пути: `ksd_ang_json/ksd_ang_####.json`, `indexes/works.json`, `indexes/raags.json`, `indexes/shabads.json`, `manifest.json`, `sevadars_ru_patches.json`.
  - Задача для KSD_AI: сделать команду/скрипт, который собирает измененные JSON-файлы, считает `sha256`, base64-кодирует JSON и отправляет одним запросом в Import API.
- [ ] Распознавать, что шабд является частью большого произведения: отделять название/заголовок от повествования, учитывать направление повествовательной дуги и связь строк внутри композиции.
- [ ] Сделать анализ тезисов по ангам: вытаскивать из `ksd_ang_json/` ключевые утверждения и находить противоречия с KSD-принципами (космология вместо внутренней реализации, Веды/Пураны как внешний авторитет, внешние люди вместо внутренних процессов, загробное/ритуальное чтение).
- [ ] Сделать техническую документацию по приложению Nitnem Authentic: архитектура, сборка APK, обновление контента, структура JSON, добавление переводчиков и будущая API-синхронизация.
- [ ] Отчётливо разграничить приложение Nitnem Authentic и KSD-аналитику: отдельные модули/пакеты, отдельная документация и понятные границы данных, чтобы позже подключить WordPress-плагин, единую API-базу, авторизацию и токены доступа без смешивания уровней.
- [ ] Добавить ночной режим чтения.
- [ ] Добавить режим чтения на английском языке.
